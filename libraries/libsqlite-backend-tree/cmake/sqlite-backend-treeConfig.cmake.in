@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
if(WIN32)
else()
    find_dependency(Threads)
endif()
include("${CMAKE_CURRENT_LIST_DIR}/sqlite-backend-treeTargets.cmake")
check_required_components(sqlite-backend-tree)
